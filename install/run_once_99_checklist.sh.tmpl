#!/bin/bash
set -e

echo "===== 🚀 Iniciando checklist do ambiente chezmoi ====="

# 1. Shell e prompt
echo "[1/8] Testando shell e prompt..."
if [[ "$SHELL" == *"zsh" ]]; then
  echo "✅ Shell padrão é Zsh"
else
  echo "❌ Shell padrão não é Zsh"
fi

if command -v starship &>/dev/null; then
  echo "✅ Starship instalado: $(starship --version)"
else
  echo "❌ Starship não encontrado"
fi

# 2. Plugins Zsh
echo "[2/8] Testando plugins Zsh..."
if [[ -n "$(typeset -f _fzf_tab_completion 2>/dev/null)" ]]; then
  echo "✅ fzf-tab carregado"
else
  echo "⚠️ fzf-tab não detectado"
fi

if [[ -n "$ZSH_AUTOSUGGEST_HIGHLIGHT_STYLE" ]]; then
  echo "✅ zsh-autosuggestions carregado"
else
  echo "⚠️ zsh-autosuggestions não detectado"
fi

if command -v zoxide &>/dev/null; then
  echo "✅ zoxide instalado"
else
  echo "❌ zoxide não encontrado"
fi

# 3. asdf e linguagens
echo "[3/8] Testando mise..."
if command -v mise &>/dev/null; then
  echo "✅ mise instalado: $(mise --version | head -n1)"

  TOOLS=(node python go java)

  for t in "${TOOLS[@]}"; do
    installed_out="$(mise ls --installed "$t" 2>/dev/null | sed -n '1p')"
    current_out="$(mise ls --current "$t" 2>/dev/null | sed -n '1p')"

    if [[ -n "$installed_out" ]]; then
      # mostra a versão real executando via mise (independe do PATH/activate)
      case "$t" in
        node)   ver="$(mise exec node  -- node -v 2>/dev/null | head -n1)" ;;
        python) ver="$(mise exec python -- python --version 2>/dev/null | head -n1)" ;;
        go)     ver="$(mise exec go     -- go version 2>/dev/null | head -n1)" ;;
        java)   ver="$(mise exec java   -- java -version 2>&1       | head -n1)" ;;
      esac
      echo "✅ $t instalado no mise — ${ver:-ok}"
    elif [[ -n "$current_out" ]]; then
      echo "⚠️  $t configurado no mise, mas não instalado (rode: mise install $t)"
    else
      echo "❌ $t não encontrado no mise"
    fi
  done
else
  echo "❌ mise não encontrado"
fi


# 4. CLIs cloud
echo "[4/8] Testando CLIs cloud..."
for cli in aws gh gcloud; do
  if command -v $cli &>/dev/null; then
    echo "✅ $cli instalado: $($cli --version | head -n1)"
  else
    echo "❌ $cli não encontrado"
  fi
done

# 5. Neovim
echo "[5/8] Testando Neovim..."
if command -v nvim &>/dev/null; then
  echo "✅ Neovim instalado: $(nvim --version | head -n1)"
  if [[ -d "$HOME/.local/share/nvim/lazy/LazyVim" ]]; then
    echo "✅ LazyVim detectado"
  else
    echo "⚠️ LazyVim não detectado"
  fi
else
  echo "❌ Neovim não encontrado"
fi

# 6. Utilitários comuns
echo "[6/8] Testando utilitários comuns..."
COMMON_TOOLS=(atuin bat btop curlie dust eza fd fzf ghostty htop hurl k9s lazydocker lazygit navi nu rg shellcheck starship yazi zoxide  )
# Nome “amigável” para exibir
declare -A FRIENDLY=(
  [nu]="nushell"
  [rg]="ripgrep"
)

# Flags padrão (na ordem)
DEFAULT_CANDIDATES=(
  "--version"
  "-V"
  "version"
  "version --short"
)

# Exceções por ferramenta
declare -A CUSTOM
CUSTOM[fzf]="-v"                 # fzf usa -v
CUSTOM[k9s]="version --short"    # k9s tem subcomando version

try_version() {
  local tool="$1"
  local out=""
  local -a candidates=()

  if [[ -n "${CUSTOM[$tool]:-}" ]]; then
    # se houver exceção, testa ela primeiro
    candidates+=("${CUSTOM[$tool]}")
  fi
  candidates+=("${DEFAULT_CANDIDATES[@]}")

  for cand in "${candidates[@]}"; do
    # divide cand em palavras (ex.: "version --short")
    # shellcheck disable=SC2206
    local args=( $cand )
    if out="$("$tool" "${args[@]}" 2>&1)"; then
      # pegamos só a 1a linha para não poluir
      echo "$out" | head -n1
      return 0
    fi
  done

  return 1
}

for tool in "${COMMON_TOOLS[@]}"; do
  if command -v "$tool" >/dev/null 2>&1; then
    if ver="$(try_version "$tool")"; then
      printf "✅ %s instalado — %s\n" "${FRIENDLY[$tool]:-$tool}" "$ver"
    else
      printf "✅ %s instalado\n" "${FRIENDLY[$tool]:-$tool}"
    fi
  else
    printf "❌ %s não encontrado\n" "${FRIENDLY[$tool]:-$tool}"
  fi
done

# 7. Validar PATH
echo "[7/8] Validando PATH..."
echo "$PATH" | tr ':' '\n'

# 9. Finalização
echo "[8/8] Checklist concluído!"
echo "===== ✅ Fim ====="
